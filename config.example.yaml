plugins:
  enabled: true
  dir: "plugins"
  configs:
    key-chain-router:
      enabled: true
      priority: 100
      state_file: key-chain-router-state.json
      ticket_ttl_seconds: 30

# Policy、模型 Rule、Weight/Priority/Failover 和可观测性均在插件中文管理页配置：
# /v0/resource/plugins/key-chain-router/status
